var num = +prompt("enter the number of which you want to the print the table");
var tableLength = + prompt("What length you prefer for the table's length");
for(var i=1;i<=tableLength;i++){
  document.write(num * i + "<br>");

}